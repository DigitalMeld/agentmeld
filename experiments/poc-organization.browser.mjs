// Explicit disposable browser fixture. No models, containers or personal files.
import { createPocServer } from '../apps/poc/server.mjs';
import { mkdtemp,rm,readFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { randomUUID } from 'node:crypto';
import assert from 'node:assert/strict';
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE||'playwright');
const directory=await mkdtemp(tmpdir()+'/agentmeld-org-ui-');
const app=await createPocServer({directory,port:0,execute:async (task,save,control,conversation)=>{task.status='completed';task.activity='Finished';task.answer='Reply '+task.prompt;task.artifacts=[{name:task.prompt==='Beta'?'data.json':'report.md',data:Buffer.from('# '+task.prompt+'\n\nOriginal output').toString('base64')}];if(task.prompt==='Category fixtures')task.artifacts.push(...[{name:'page.html',data:Buffer.from('<h1>Safe HTML</h1>').toString('base64')},{name:'image.gif',data:'R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7'},{name:'video.mp4',data:Buffer.from('video fixture').toString('base64')},{name:'audio.mp3',data:Buffer.from('audio fixture').toString('base64')}]);conversation.workspace=[...task.artifacts,{name:'nested',directory:true},{name:'nested/helper.js',data:Buffer.from('const helper = true;').toString('base64')},{name:'.notes',data:Buffer.from('workspace notes').toString('base64')}];conversation.workspaceCapturedAt=new Date().toISOString();}});
const headers={Authorization:'Bearer '+app.token,'Content-Type':'application/json'};
async function submit(prompt,conversationId=null,files=[]){const t=await(await fetch(app.origin+'/api/tasks',{method:'POST',headers,body:JSON.stringify({prompt,conversationId,files,requestKey:randomUUID()})})).json();for(let n=0;n<100;n++){const state=await(await fetch(app.origin+'/api/state',{headers})).json();if(!state.active&&!state.tasks.some(t=>t.status==='queued'))return t;await new Promise(r=>setTimeout(r,10));}throw Error('Fixture did not settle');}
let browser;
try{
 const alpha=await submit('Alpha',null,[{name:'original.txt',data:Buffer.from('Original attachment').toString('base64')}]);await submit('Revision',alpha.conversationId);await submit('Beta');
 browser=await chromium.launch({channel:'chrome',chromiumSandbox:true});const page=await browser.newPage({viewport:{width:1440,height:900}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
 await page.goto(app.origin+'/#'+app.token);await page.locator('.historyItem').first().waitFor();
 assert.equal(await page.locator('.historyItem .chatTitle').first().innerText(),'Beta');
 await page.locator('.historyItem').filter({has:page.locator('.chatTitle',{hasText:'Alpha'})}).click();
 assert.equal(await page.locator('#chatSummary').innerText(),'2 turns');
 const inputDownload=page.waitForEvent('download');await page.locator('[data-input]').click();const input=await inputDownload;assert.equal(await readFile(await input.path(),'utf8'),'Original attachment');
 await page.locator('#chatOptionsButton').click();await page.locator('#chatName').fill('Renamed Alpha');await page.locator('#renameChat button').click();await page.waitForFunction(()=>document.querySelector('#selectedTitle').textContent==='Renamed Alpha');
 await page.locator('#pinChat').click();await page.waitForFunction(()=>document.querySelector('#pinChat').textContent==='Unpin chat');
 const jsonDownload=page.waitForEvent('download');await page.locator('[data-export="json"]').click();const json=JSON.parse(await readFile(await(await jsonDownload).path(),'utf8'));assert.equal(json.conversation.title,'Renamed Alpha');assert.equal(json.turns.length,2);assert.ok(!JSON.stringify(json).includes('Original attachment'));
 const mdDownload=page.waitForEvent('download');await page.locator('[data-export="md"]').click();assert.match(await readFile(await(await mdDownload).path(),'utf8'),/# Renamed Alpha/);
 await page.locator('#archiveChat').click();await page.waitForFunction(()=>document.querySelector('#archiveChat').textContent==='Restore chat');await page.locator('#closeChatOptions').click();
 assert.equal(await page.locator('#prompt').isDisabled(),true);assert.equal(await page.locator('#archiveChats').getAttribute('aria-pressed'),'true');
 await page.locator('#archiveChats').click();assert.equal(await page.locator('.historyItem').count(),1);
 await page.locator('#archiveChats').focus();await page.keyboard.press('Enter');await page.locator('#chatSearch').fill('RENAMED');assert.equal(await page.locator('.historyItem').count(),1);await page.locator('#chatSearch').fill('');
 assert.equal(await page.locator('#chatHeading').innerText(),'Archived');assert.equal(await page.locator('#archiveChats').getAttribute('aria-label'),'Show active chats');assert.equal(await page.locator('#chatScope').count(),0);
 await page.screenshot({path:'.local/m0/organized-archive.png'});
 await page.reload();await page.locator('#restoreChat').waitFor();assert.equal(await page.locator('#archiveChats').getAttribute('aria-pressed'),'true');
 await page.locator('#restoreChat').click();await page.waitForFunction(()=>!document.querySelector('#prompt').disabled);assert.equal(await page.locator('.historyItem .chatTitle').first().innerText(),'Renamed Alpha');
 await page.locator('#filesNav').click();await page.locator('[data-category="documents"]').click();await page.locator('#fileSearch').fill('report.md');assert.equal(await page.locator('.libraryEntry').count(),2);assert.match(await page.locator('#fileSummary').innerText(),/^2 outputs/);
 await page.locator('#fileOptions summary').click();await page.locator('[data-sort="oldest"]').click();assert.match(await page.locator('.outputOrigin').first().innerText(),/Version 1/);
 await page.locator('#fileOptions summary').click();await page.locator('[data-sort="newest"]').click();assert.match(await page.locator('.outputOrigin').first().innerText(),/Version 2/);
 await page.locator('#fileOptions summary').click();await page.screenshot({path:'.local/m0/artifacts-options.png'});assert.equal(await page.locator('#fileOptions select').count(),0);await page.keyboard.press('Escape');assert.equal(await page.locator('#fileOptions').getAttribute('open'),null);
 await page.locator('#libraryFiles .file').first().click();await page.locator('#preview[open][aria-busy="false"]').waitFor();assert.equal(await page.locator('#previewBody h1').innerText(),'Revision');
 await page.locator('#previewSource').click();assert.match(await page.locator('#previewBody pre').innerText(),/^# Revision/);
 await page.locator('#previewVersion').selectOption(alpha.id);await page.waitForFunction(()=>document.querySelector('#previewBody h1')?.textContent==='Alpha');
 await page.screenshot({path:'.local/m0/organized-preview.png'});
 await page.locator('#previewConversation').click();assert.ok((await page.locator('.turn:focus').innerText()).includes('Alpha'));
 await page.locator('#activitySearch').fill('Revision');assert.equal(await page.locator('.activityEntry').count(),1);await page.locator('#activitySearch').fill('');
 await page.keyboard.press('Control+k');assert.equal(await page.locator('#chatSearch').evaluate(e=>e===document.activeElement),true);
 await page.locator('#shortcutHelp').click();await page.locator('#shortcuts[open]').waitFor();await page.locator('#closeShortcuts').click();
 await page.locator('#prompt').fill('Unsent draft');const warning=page.waitForEvent('dialog');const reloading=page.reload();const dialog=await warning;assert.equal(dialog.type(),'beforeunload');await dialog.dismiss();await reloading.catch(()=>{});assert.equal(await page.locator('#prompt').inputValue(),'Unsent draft');await page.locator('#prompt').fill('');
 await page.evaluate(()=>{document.querySelector('#conversation').style.height='150px';document.querySelector('#conversation').style.flex='none';document.querySelector('#conversation').scrollTop=0;document.querySelector('#conversation').dispatchEvent(new Event('scroll'));});
 await page.locator('#jumpLatest').click();assert.equal(await page.locator('#latestBar').isVisible(),false);
 await page.setViewportSize({width:390,height:844});await page.locator('#chatOptionsButton').click();await page.screenshot({path:'.local/m0/organized-mobile.png'});assert.equal(await page.locator('#chatOptions').isVisible(),true);await page.locator('#closeChatOptions').click();
 await page.evaluate(()=>{document.querySelector('#conversation').style.removeProperty('height');document.querySelector('#conversation').style.removeProperty('flex');});
 await page.setViewportSize({width:1440,height:900});await page.locator('#filesNav').click();
 assert.equal(await page.locator('.history').isVisible(),false);assert.equal(await page.locator('#inspector').isVisible(),false);assert.equal(await page.locator('.top').isVisible(),false);
 assert.equal(await page.locator('#createArtifact').count(),0);assert.equal(await page.locator('#library #fileSearch').count(),0);assert.equal(await page.locator('#fileSidebar #fileSearch').isVisible(),true);
 await page.locator('#fileSearch').fill('');await page.locator('[data-category="all"]').click();await page.waitForFunction(()=>document.querySelector('.thumbnailText')?.textContent.includes('Original output'));
 await page.screenshot({path:'.local/m0/artifacts-grid.png'});
 await page.locator('[data-category="documents"]').click();assert.equal(await page.locator('#libraryFiles').getAttribute('class'),'artifactList');await page.screenshot({path:'.local/m0/artifacts-documents.png'});
 await page.locator('[data-category="system"]').click();await page.waitForFunction(()=>document.querySelector('#workspaceStatus').textContent.includes('All saved files'));assert.equal(await page.locator('#libraryFiles tbody tr').count(),3);assert.equal(await page.locator('#workspaceTools').isVisible(),true);await page.screenshot({path:'.local/m0/artifacts-system.png'});
 for(const category of ['web','images','videos','audio']){await page.locator('[data-category="'+category+'"]').click();assert.equal(await page.locator('.libraryEntry').count(),0);assert.equal(await page.locator('.libraryEmpty svg').count(),0);assert.match(await page.locator('.libraryEmpty h2').innerText(),/^No .+ yet$/);}
 await page.screenshot({path:'.local/m0/artifacts-empty.png'});
 await page.locator('[data-category="all"]').click();await page.keyboard.press('Control+k');assert.equal(await page.locator('#fileSearch').evaluate(e=>e===document.activeElement),true);await page.keyboard.press('Escape');
 await page.locator('#libraryChat').click();assert.equal(await page.locator('#prompt').isVisible(),true);assert.equal(await page.locator('.history').isVisible(),false);
 await page.locator('#prompt').fill('Draft beside files');await page.locator('#fileChatClose').click();await page.locator('#libraryChat').click();assert.equal(await page.locator('#prompt').inputValue(),'Draft beside files');
 await page.screenshot({path:'.local/m0/artifacts-chat.png'});
 await page.locator('#send').click();await page.waitForFunction(()=>[...document.querySelectorAll('.message.assistant')].some(e=>e.textContent.includes('Reply Draft beside files')));
 assert.equal(await page.locator('#library').isVisible(),true);await page.locator('#fileChatClose').click();
 await page.setViewportSize({width:390,height:844});await page.locator('#libraryMenu').click();await page.locator('[data-category="documents"]').click();assert.equal(await page.locator('#fileSidebar').isVisible(),false);
 await page.locator('#libraryChat').click();assert.equal(await page.locator('#prompt').isVisible(),true);await page.screenshot({path:'.local/m0/artifacts-mobile-chat.png'});await page.locator('#fileChatClose').click();
 assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);
 await page.setViewportSize({width:1440,height:900});
 await submit('Category fixtures');await page.locator('[data-category="all"]').click();await page.waitForFunction(()=>[...document.querySelectorAll('.file')].some(e=>e.textContent.includes('image.gif')));
 for(const category of ['all','documents','web','images','videos','audio']){
  await page.locator('[data-category="'+category+'"]').click();assert.ok(await page.locator('.libraryEntry').count()>0);
  for(const layout of ['grid','list']){await page.locator('#fileOptions summary').click();await page.locator('[data-layout="'+layout+'"]').click();assert.equal(await page.locator('#libraryFiles').getAttribute('class'),layout==='grid'?'artifactGrid':'artifactList');}
  await page.locator('#fileOptions summary').click();await page.locator('[data-layout="grid"]').click();
 }
 await page.locator('[data-category="images"]').click();await page.locator('#libraryFiles .file').first().click();await page.locator('#preview[open][aria-busy="false"]').waitFor();await page.waitForFunction(()=>document.querySelector('.fileImage')?.naturalWidth===1);await page.locator('#closePreview').click();
 await page.locator('[data-category="all"]').click();await page.locator('#fileOptions summary').click();await page.locator('[data-sort="opened"]').click();assert.ok((await page.locator('#libraryFiles .file').first().innerText()).includes('image.gif'));
 await page.locator('[data-category="documents"]').click();assert.equal(await page.locator('#libraryFiles').getAttribute('class'),'artifactGrid');assert.equal(await page.locator('.artifactThumbnail').first().isVisible(),true);
 await page.screenshot({path:'.local/m0/documents-grid.png'});
 await page.locator('#fileOptions summary').click();await page.keyboard.press('ArrowDown');assert.equal(await page.locator('[data-sort="opened"]').evaluate(e=>e===document.activeElement),true);await page.keyboard.press('End');assert.equal(await page.locator('[data-layout="list"]').evaluate(e=>e===document.activeElement),true);await page.keyboard.press('Enter');
 assert.equal(await page.locator('.artifactThumbnail').first().isVisible(),false);assert.equal(await page.locator('#libraryFiles').evaluate(e=>getComputedStyle(e).display),'block');await page.screenshot({path:'.local/m0/documents-list.png'});
 await page.locator('#fileOptions summary').click();await page.locator('[data-sort="name"]').click();await page.reload();await page.locator('#filesNav').click();await page.locator('[data-category="documents"]').click();assert.equal(await page.locator('#libraryFiles').getAttribute('class'),'artifactList');assert.equal(await page.locator('[data-sort="name"]').getAttribute('aria-pressed'),'true');
 await page.locator('[data-category="system"]').click();await page.waitForFunction(()=>document.querySelector('#workspaceStatus').textContent.includes('All saved files'));
 await page.locator('[data-folder="nested"]').click();assert.equal(await page.locator('#libraryFiles tbody tr').count(),1);await page.locator('[data-workspace-file="nested/helper.js"]').click();await page.locator('#preview[open][aria-busy="false"]').waitFor();assert.match(await page.locator('#previewBody').innerText(),/const helper = true/);
 const workspaceDownload=page.waitForEvent('download');await page.locator('#downloadFile').click();assert.equal(await readFile(await(await workspaceDownload).path(),'utf8'),'const helper = true;');await page.locator('#closePreview').click();
 await page.locator('#workspaceUp').click();await page.locator('#showHiddenFiles').uncheck();assert.equal(await page.locator('[data-workspace-file=".notes"]').count(),0);await page.locator('#showHiddenFiles').check();assert.equal(await page.locator('[data-workspace-file=".notes"]').count(),1);
 await page.locator('#fileSearch').fill('helper');assert.equal(await page.locator('[data-workspace-file="nested/helper.js"]').count(),1);await page.locator('#fileSearch').fill('');
 await page.route('**/api/workspace?*',route=>route.abort());await page.locator('#workspaceRefresh').click();await page.locator('#workspaceError:not([hidden])').waitFor();await page.unroute('**/api/workspace?*');await page.locator('#workspaceRefresh').click();await page.waitForFunction(()=>document.querySelector('#workspaceStatus').textContent.includes('All saved files'));assert.equal(await page.locator('#workspaceError').isVisible(),false);
 await page.locator('#workspacePicker summary').click();await page.locator('[data-workspace-id]').filter({hasText:'Beta'}).click();await page.waitForFunction(()=>document.querySelector('#workspaceLabel').textContent==='Beta'&&document.querySelector('#workspaceStatus').textContent.includes('All saved files'));
 assert.equal(await page.locator('#fileOptions').isVisible(),false);
 await page.screenshot({path:'.local/m0/full-workspace.png'});
 assert.deepEqual(errors,[]);console.log('Organization browser passed: rename/pin/archive/restore, exports and originals, versions, filters, shortcuts, unsent warning, mobile.');
}finally{if(browser)await browser.close();await app.shutdown();await rm(directory,{recursive:true,force:true});}
